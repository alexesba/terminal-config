# README screenshots

Images linked from the root [README](../../README.md). Update them manually when the UI changes.

| File | Description |
|------|-------------|
| `colorscheme.png` | Full-screen `colorscheme` picker with live preview |
| `install-summary.png` | `install.sh` pre-flight summary before `Proceed?` |

Capture from WezTerm (or your terminal of choice) and replace the PNGs in this folder.
